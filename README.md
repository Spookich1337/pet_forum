запуск проекта

docker compose up --build

api: http://localhost:8080/...

client: http://localhost:8080/

стэк: postgresql, fastapi, pydantic, redis, docker/docker-compose

в работе: websocket, доработать rabbitmq + celery, alembic
